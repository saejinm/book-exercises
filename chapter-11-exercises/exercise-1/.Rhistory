install.packages("devtools")
install.packages("devtools")
# Install "fueleconomy" dataset from GitHub
devtools::install_github("hadley/fueleconomy")
# Use the `libary()` function to load the "fueleconomy" package
library(fueleconomy)
vehicles_1997$average <- (vehicles_1997$highway + vehicles_1997$city)/2
vehicles_1997$average <- (vehicles_1997$hwy + vehicles_1997$city)/2
vehicles_2 <- vehicles[vehicles$drive == '2-wheel Drive', ]
efficient_2 <- vehicles_2[vehicles_2 = 20]
print(efficient_2)
vehicles_2 <- vehicles[vehicles$drive == "2-Wheel Drive", ]
efficient_2 <- vehicles_2[vehicles_2 = 20]
print(efficient_2)
vehicles_2 <- vehicles[vehicles$drive == "2-Wheel Drive", ]
efficient_2 <- vehicles_2[vehicles_2 = 20, ]
print(efficient_2)
vehicles_2nd[vehicles_2nd$hwy == min(vehicles_2nd$hwy), ]
vehicles_1997$average <- (vehicles_1997$hwy + vehicles_1997$cty)/2
views(vehicles_1997)
vehicles_1997$average <- (vehicles_1997$hwy + vehicles_1997$cty)/2
views(vehicles_1997$average)
# Exercise 1: working with data frames (review)
# Install devtools package: allows installations from GitHub
install.packages("devtools")
# Install "fueleconomy" dataset from GitHub
devtools::install_github("hadley/fueleconomy")
# Use the `libary()` function to load the "fueleconomy" package
library(fueleconomy)
# You should now have access to the `vehicles` data frame
# You can use `View()` to inspect it
# Select the different manufacturers (makes) of the cars in this data set.
# Save this vector in a variable
# Use the `unique()` function to determine how many different car manufacturers
# are represented by the data set
# Filter the data set for vehicles manufactured in 1997
# Arrange the 1997 cars by highway (`hwy`) gas milage
# Hint: use the `order()` function to get a vector of indices in order by value
# See also:
# https://www.r-bloggers.com/r-sorting-a-data-frame-by-the-contents-of-a-column/
# Mutate the 1997 cars data frame to add a column `average` that has the average
# gas milage (between city and highway mpg) for each car
vehicles_1997$average <- (vehicles_1997$hwy + vehicles_1997$cty)/2
views(vehicles_1997$average)
install.packages("devtools")
library(fueleconomy)
# You should now have access to the `vehicles` data frame
# You can use `View()` to inspect it
# Select the different manufacturers (makes) of the cars in this data set.
# Save this vector in a variable
# Use the `unique()` function to determine how many different car manufacturers
# are represented by the data set
# Filter the data set for vehicles manufactured in 1997
# Arrange the 1997 cars by highway (`hwy`) gas milage
# Hint: use the `order()` function to get a vector of indices in order by value
# See also:
# https://www.r-bloggers.com/r-sorting-a-data-frame-by-the-contents-of-a-column/
# Mutate the 1997 cars data frame to add a column `average` that has the average
# gas milage (between city and highway mpg) for each car
vehicles_1997$average <- (vehicles_1997$hwy + vehicles_1997$cty)/2
views(vehicles_1997$average)
vehicles_2nd[vehicles_2nd$hwy == min(vehicles_2nd$hwy), "id"]
vehicles_2wd[vehicles_2wd$hwy == min(vehicles_2wd$hwy), "id"]
install.packages("dplyr")
